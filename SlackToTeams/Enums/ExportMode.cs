namespace SlackToTeams.Enums {
    #region ExportMode

    public enum ExportMode {
        TeamsHtml = 0,  // Will export to Teams and Create HTML
        Teams = 1,      // Will export to Teams only
        Html = 2        // Will create HTML only
    }

    #endregion
}
